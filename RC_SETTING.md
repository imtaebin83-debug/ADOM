# Traxxas XL-5 RC 설정

이 문서는 Traxxas XL-5 ESC(`#3018R`)의 스로틀 엔드포인트를 ADOM의
PCA9685 출력에 다시 학습시키는 절차를 정리한다. 기준 매뉴얼은
`~/Downloads/KC1761-R01_3018R-XL5_INST_140430-EN.pdf`의
**Setup Programming** 절이다.

## 현재 차량 전원 조건

> **중요:** 이 차량은 제어 계통 전원과 모터 구동용 배터리가 별개다.
> ROS 노드와 PCA9685가 동작하더라도 **구동용 배터리가 XL-5에 연결되어
> 있는지 반드시 별도로 확인한다.**

현재 구동용 배터리는 **NiMH**다. 따라서 XL-5의 Low-Voltage Detection은
비활성화되어야 하며, 전원이 켜지고 스로틀이 중립인 정상 대기 상태에서
XL-5 LED는 **정적으로 켜진 빨간색(solid red)**이어야 한다.

- 정적 빨간 LED: NiMH 설정, Low-Voltage Detection 비활성화 — 현재 정상 상태
- 정적 녹색 LED: LiPo 설정, Low-Voltage Detection 활성화 — 현재 배터리 설정과 다름
- 빠르게 깜박이는 빨간 LED: 과열 보호 상태
- 느리게 깜박이는 빨간 LED: 저전압 보호 상태

LED가 정적 녹색이라면 엔드포인트 학습과 혼동하지 말고 배터리 모드를 먼저
확인한다. 배터리 종류를 확인하지 않은 상태에서 설정을 바꾸지 않는다.

## 엔드포인트 학습 전 준비

XL-5는 중립, 최대 전진, 최대 브레이크/후진 입력을 학습한다. 이 과정은
차량의 실제 최고속도를 지정하는 과정이 아니다.

1. 차량의 구동 바퀴를 모두 지면에서 띄운다.
2. 가능하면 XL-5와 모터 사이의 모터선 하나를 분리한다.
3. 비상시 즉시 분리할 수 있도록 구동용 배터리 커넥터에 접근할 수 있게 한다.
4. 제어 계통 전원과 ROS 노드를 켜고 PCA9685 출력을 확인한다.
5. **별도의 NiMH 구동용 배터리가 충전되어 있고 XL-5에 연결되어 있는지 확인한다.**
6. 다음 PWM 엔드포인트를 사용한다.

```yaml
esc_neutral_us: 1500.0
esc_forward_max_us: 2000.0
esc_reverse_max_us: 1000.0
```

다음 명령으로 실제 PWM 출력을 확인할 수 있다.

```bash
ros2 topic echo /adom/control/pwm_us
```

게임패드를 사용한다면 `X` 버튼으로 Manual 모드에 들어간 뒤 두 스틱을
중앙으로 한 번 복귀시켜 수동 조작을 arm한다. 학습을 시작하기 전까지
스로틀을 중앙에 유지하고 약 `1500 us`가 출력되는지 확인한다.

## XL-5 엔드포인트 재학습

1. XL-5가 꺼진 상태에서 충전된 **NiMH 구동용 배터리**가 XL-5에 연결되어
   있는지 다시 확인한다.
2. PCA9685에서 중립인 약 `1500 us`를 안정적으로 출력한다.
3. XL-5의 `EZ-Set` 버튼을 길게 누른다.
4. LED가 녹색에서 빨간색으로 바뀌면 즉시 `EZ-Set` 버튼을 놓는다.
5. 약 3초 뒤 LED가 빨간색으로 한 번 깜박이면 최대 전진 입력을 넣고
   유지한다.
   - 게임패드: 전진 방향 끝까지
   - 예상 PWM: 약 `2000 us`
6. 약 3초 뒤 LED가 빨간색으로 두 번 깜박이면 최대 브레이크/후진 입력을
   넣고 유지한다.
   - 게임패드: 후진 방향 끝까지
   - 예상 PWM: 약 `1000 us`
7. LED가 녹색으로 한 번 깜박이면 학습이 완료된 것이다.
8. 즉시 스로틀을 중앙으로 돌려 약 `1500 us`로 복귀시킨다.
9. NiMH 설정에서는 완료 후 중립 대기 LED가 **깜박이지 않는 정적 빨간색**인지
   확인한다.

LED 순서가 예상과 다르거나 과정 중 입력을 잘못 넣었다면 구동용 배터리를
분리하고 몇 초 기다린 다음 처음부터 다시 진행한다.

## 주의 사항

- 엔드포인트 학습 중에는 `1700 us` 같은 부분 입력을 최대 전진 위치로
  사용하지 않는다. XL-5가 그 입력을 100%로 학습하여 조작 범위가 좁아진다.
- 실제 최고속도는 엔드포인트 학습을 정상 범위로 완료한 뒤
  `esc_forward_max_us`와 `esc_reverse_max_us`를 보수적으로 조정하면서
  별도로 실측한다.
- 후진은 XL-5의 브레이크-중립-후진 동작을 바퀴가 들린 상태에서 먼저
  확인한다.
- 소프트웨어 정지와 ROS watchdog은 물리적인 구동 배터리 분리를 대체하지
  않는다.
