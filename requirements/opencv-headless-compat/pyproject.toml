[build-system]
requires = ["setuptools==69.5.1", "wheel==0.41.2"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-python-headless"
version = "4.8.0.76"
description = "Metadata-only compatibility shim; cv2 is provided by opencv-python"

[tool.setuptools]
packages = []
