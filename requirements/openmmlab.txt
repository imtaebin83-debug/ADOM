# mmengine, mmcv, and albumentations are installed with --no-deps in the
# Dockerfile after this pinned runtime stack has been installed.
mmsegmentation==1.2.2
mmdeploy==1.3.1
onnx==1.15.0
onnxruntime==1.16.3
opencv-python==4.8.0.76
numpy==1.24.4
scipy==1.11.1
matplotlib==3.8.0
tqdm==4.66.1
tensorboard==2.14.1
tensorboard-data-server==0.7.2
# Newer W&B releases require protobuf>=5, while MMDeploy 1.3.1 requires
# protobuf<=3.20.2. W&B 0.22.3 supports protobuf 3.19+ and Python 3.10.
wandb==0.22.3
protobuf==3.20.2
grpcio==1.59.0
pycocotools==2.0.7
pandas==1.5.3
scikit-learn==1.3.2
# Albumentations 1.3.1 runtime dependencies. Albumentations itself is installed
# with qudida and --no-deps so neither can add opencv-python-headless.
scikit-image==0.21.0
# Explicit OpenMMLab runtime dependencies used during the no-deps bootstrap.
addict==2.4.0
packaging==23.1
prettytable==3.9.0
PyYAML==6.0.1
rich==13.4.2
termcolor==2.3.0
yapf==0.40.2
# MMSegmentation 1.2.2 imports its bundled CLIP tokenizer when
# mmseg.datasets is imported, but these dependencies are absent from the
# package's runtime metadata. Keep them explicit so the --no-deps bootstrap
# cannot produce an image that passes pip check but fails at runtime.
ftfy==6.1.1
regex==2023.10.3
